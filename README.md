# dev-project-management
Par Dano

le projet est encore en cours de développement et ne fonctionne pas encore

# Important pour utiliser le projet
Le projet utilise MongoDB
il faut remplacer les identifiants présents dans le fichier env par vos identifiants MongoDB

1. rename file (macos) -> cp .env.exemple .env.local
2. Add your own MongoDB identifiants in the .env.local file